@model IEnumerable<BulkyBook.Models.CoverType>
@{
    ViewData["Title"] = "Index";
}


<div class="Container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">CoverType List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-action="Create" asp-controller="CoverType" class="btn btn-primary">
                Create New CoverType
            </a>
        </div>
    </div>


<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Cover Type
            </th>
            <th>

            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td width="50%">
                    @item.Name
                </td>
                <td class="text-end">
                    <div class="w-50 btn-group text-end" role="group">
                        <a asp-action="Edit" asp-controller="CoverType" asp-route-id="@item.Id" class="btn btn-primary mx-2">Edit</a>
                    </div>
                </td>
                <td class="text-end">
                    <div class="w-50 btn-group text-end" role="group">
                        <a asp-action="Delete" asp-controller="CoverType" asp-route-id="@item.Id" class="btn btn-primary mx-2">Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>